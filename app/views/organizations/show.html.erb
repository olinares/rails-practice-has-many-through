<div class="page-header">
  <h1>Showing Organization <%= @organization.name %></h1>
</div>

<h2>People</h2>

<table>
  <thead>
    <tr>
      <th>
        Name
      </th>
    </tr>
  </thead>
  <tbody>
    <div class="well">
      <% @organization.people.each do |people| %>
      <li> <%= link_to people.name, people %> </li>
      <% end %>
    </div>
  </tbody>
</table>
